@startuml
title Activity: Delete tag

start

:Execute `tag delete`;

if () then ([tag exist])
else ([else])
    :Show error message;
    stop
endif

:Remove tag from tag list;
:Remove tag from all contacts using it;

stop
@enduml
